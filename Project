df_formatted = spark.createDataFrame(
    [(row.value.replace(field_separator, " | "),) for row in data_lines],
    ["row_text"]
)

df_formatted.show(truncate=False)
source_count = df_formatted.count()
