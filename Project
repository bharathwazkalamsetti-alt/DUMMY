Gather:
Merges multiple GFCID segment files into a single continuous flow of records.

sort :
Sorts all gathered records by client ID, batch date, country, month, and year.
Rollup component:
Summarizes multiple records into one per client, country, and time period by aggregating key attributes
PBKS:
Repartitions and sorts data by business keys before writing to the final GFCID list file.

GFCID_LIST_FILE:
The GFCID List Creation process consolidates multiple segment-level GFCID datasets, sorts and aggregates them by client, batch, and period, and outputs a single unified GFCID list file ready for downstream KYC and regulatory data consumption.
