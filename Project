The Reformat-1 component adds a new field batch_dt (value: 20171129052135) to stamp each record with the batch timestamp while passing all other fields unchanged. The Batch File component then writes this enriched record into a pipe-delimited flat file ($FILE) containing batch_dt, batch_id, process_date, rgn_id, and cntry_cd for audit and batch tracking
