from pyspark.sql.functions import split, col

df_raw = spark.createDataFrame([(r.value,) for r in data_lines], ["value"]) \
               .withColumn("cols", split(col("value"), field_separator)) \
               .select("cols")
