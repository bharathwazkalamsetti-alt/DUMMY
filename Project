The Input File component reads the incoming data file containing header, detail, and trailer records.
It defines the file structure using metadata and loads the data into the graph.
This data is then passed to the PBE – Separate Header Trailer Detail component for record-type separation.
