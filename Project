Generate Records: creates one dummy record (num_records=1; fields line='|', newline='\n') to trigger the flow.

Reformat: applies ${TRANSFORM1} (single transform; abort-on-first-reject) to build the batch metadata record (e.g., batch_id, rgn_id='NAM', cntry_cd='US', defaults/validations applied).

Replicate: duplicates the metadata record—one copy for DB insert, one for the file branch.

Output Table: inserts into Oracle using $DBC_FILE and ${TABLE_NAME} with DML ${TBL_DML}/${TBL_DML_TYPE} for audit/process tracking
